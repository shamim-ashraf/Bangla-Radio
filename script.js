var S = {
    popular: [
        { n: "Radio Today", f: "89.6 FM — Dhaka", s: "https://stream.zenolive.com/8wv4d8g4344tv", c: ["#7b1fa2","#4a148c"], i: "🎙️" },
        { n: "Peoples Radio", f: "91.6 FM — Dhaka", s: "https://s3.myradiostream.com:14498/listen.mp3", c: ["#00796b","#004d40"], i: "👥" },
        { n: "Radio Shadhin", f: "92.4 FM — Dhaka", s: "https://stream.zenolive.com/umq9q5uuva5tv", c: ["#2e7d32","#1b5e20"], i: "✊" },
        { n: "Radio Foorti", f: "88.0 FM", w: "https://radiofoorti.fm", c: ["#e65100","#bf360c"], i: "🎧" },
        { n: "ABC Radio", f: "89.2 FM", w: "https://abcradio.fm", c: ["#1565c0","#0d47a1"], i: "📰" },
        { n: "Bangladesh Betar", f: "জাতীয় রেডিও", w: "https://betar.gov.bd", c: ["#c62828","#b71c1c"], i: "🇧🇩" },
        { n: "Jago FM", f: "94.4 FM", w: "https://jagofm.com", c: ["#f57f17","#e65100"], i: "⚡" },
        { n: "Betar বাণিজ্যিক", f: "বেতার বাণিজ্যিক", w: "https://betar.gov.bd", c: ["#880e4f","#4a148c"], i: "📡" },
    ],
    islamic: [
        { n: "কুরআন তারাতীল", f: "কুরআন তিলাওয়াত ২৪/৭", s: "https://Qurango.net/radio/tarateel", c: ["#2e7d32","#0a3d0a"], i: "📖" },
        { n: "Abdul Basit", f: "কুরআন — আব্দুল বাসিত", s: "https://radio.mp3islam.com/listen/abdulbasit/radio.mp3", c: ["#1b5e20","#0a3d0a"], i: "🕌" },
        { n: "Al-Minshawi", f: "কুরআন — আল মিনশাউয়ী", s: "https://qurango.net/radio/mohammed_siddiq_alminshawi", c: ["#33691e","#1b5e20"], i: "☪️" },
        { n: "Maher Al Muaiqly", f: "কুরআন — মাহের আল মুয়াইক্বলী", s: "https://Qurango.net/radio/maher_al_muaiqly", c: ["#1a5c1a","#0a3d0a"], i: "🕋" },
    ],
    music: [
        { n: "Mellow Bangla", f: "Folk & Romantic", s: "https://radio.mellowbangla.com/stream", c: ["#ad1457","#880e4f"], i: "💜" },
        { n: "A2Z Radio", f: "Mix Music", s: "https://listen.radioking.com/radio/1743/stream/125", c: ["#6a1b9a","#4a148c"], i: "🎶" },
        { n: "Radio Ullash", f: "উল্লাস", s: "https://stream.zeno.fm/gpgkgp9mp18uv.mp3", c: ["#c62828","#b71c1c"], i: "😊" },
        { n: "S-Radio", f: "Online Music", s: "https://as1.digitalsynapsebd.com/proxy/zkpnarpg/stream", c: ["#283593","#1a237e"], i: "🎵" },
        { n: "Radio GoonGoon", f: "Music & Talk", s: "https://audio.streamcast.xyz/listen/radiogoongoon/radio.mp3", c: ["#00695c","#004d40"], i: "🔔" },
    ],
    news: [
        { n: "Radio Bhumi", f: "92.8 FM — Community", s: "https://stream.zeno.fm/be5bqmk1k18uv", c: ["#2e7d32","#1b5e20"], i: "🌳" },
        { n: "Radio Vivid Voice", f: "Talk & Music", s: "https://stream.zeno.fm/xuin6ocdc2juv", c: ["#1565c0","#0d47a1"], i: "🎤" },
        { n: "Rajshahi FM", f: "রাজশাহী", s: "https://stream.zeno.fm/nxfshdayjrdvv", c: ["#4e342e","#3e2723"], i: "📍" },
        { n: "Uttorbango Betar", f: "উত্তরবঙ্গ", s: "https://stream.zeno.fm/spqbxkqxdf9uv", c: ["#37474f","#263238"], i: "⛰️" },
    ],
    english: [
        { n: "BBC World Service", f: "English — News", s: "https://stream.live.vc.bbcmedia.co.uk/bbc_world_service", c: ["#b71c1c","#880e4f"], i: "🌍" },
        { n: "BBC South Asia", f: "English — South Asia", s: "https://stream.live.vc.bbcmedia.co.uk/bbc_world_service_south_asia", c: ["#c62828","#b71c1c"], i: "🇬🇧" },
    ],
    kolkata: [
        { n: "My 90s Bengali", f: "৯০-এর বাংলা গান", s: "https://stream.zeno.fm/2k6xfu1pvs8uv", c: ["#e65100","#bf360c"], i: "🎵" },
        { n: "MY CLUB REMIX", f: "বাংলা রিমিক্স", s: "https://stream.zeno.fm/u7eee0hyzp8uv", c: ["#ad1457","#880e4f"], i: "🔥" },
        { n: "Radio Quarantine", f: "কলকাতা কমিউনিটি", s: "https://stream.zeno.fm/krk4kw830tzuv", c: ["#283593","#1a237e"], i: "🎙️" },
        { n: "Ankora Radio", f: "বাংলা — কলকাতা", s: "https://stream.zeno.fm/xush70b1cbruv", c: ["#00695c","#004d40"], i: "📻" },
    ],
    sports: [
        { n: "Radio Bhumi", f: "ক্রিকেট ও ফুটবল কভারেজ", s: "https://stream.zeno.fm/be5bqmk1k18uv", c: ["#2e7d32","#1b5e20"], i: "⚽" },
        { n: "Radio Today", f: "খেলার ধারাবিবরণী", s: "https://stream.zenolive.com/8wv4d8g4344tv", c: ["#7b1fa2","#4a148c"], i: "🏏" },
        { n: "Peoples Radio", f: "লাইভ ক্রিকেট আপডেট", s: "https://s3.myradiostream.com:14498/listen.mp3", c: ["#00796b","#004d40"], i: "🏏" },
        { n: "BBC World Service", f: "ইংলিশ ক্রিকেট কমেন্ট্রি", s: "https://stream.live.vc.bbcmedia.co.uk/bbc_world_service", c: ["#b71c1c","#880e4f"], i: "🌍" },
        { n: "Bangladesh Betar", f: "জাতীয় ক্রিকেট কমেন্ট্রি", w: "https://betar.gov.bd", c: ["#1b5e20","#0a3d0a"], i: "🇧🇩" },
    ]
};

var audio = null, curCard = null, curStation = null, sleepTimer = null, connectTimer = null, connectSec = 0;

function getFavs() { try { return JSON.parse(localStorage.getItem("br_favs") || "[]"); } catch(e) { return []; } }
function saveFavs(f) { try { localStorage.setItem("br_favs", JSON.stringify(f)); } catch(e) {} }
function isFav(n) { return getFavs().indexOf(n) !== -1; }
function toggleFav(n) { var f = getFavs(), x = f.indexOf(n); if (x===-1) f.push(n); else f.splice(x,1); saveFavs(f); return x===-1; }
function saveLastPlayed(st) { try { localStorage.setItem("br_last", JSON.stringify(st)); } catch(e) {} }
function getLastPlayed() { try { return JSON.parse(localStorage.getItem("br_last")); } catch(e) { return null; } }

function mk(t, c, h) { var e = document.createElement(t); if(c) e.className=c; if(h) e.innerHTML=h; return e; }

function renderGrid(cat) {
    var grid = document.getElementById("grid"), wrap = document.getElementById("gridWrap");
    grid.innerHTML = ""; wrap.scrollTop = 0;
    var ob = document.querySelector(".cbanner"); if(ob) ob.remove();

    if (cat === "sports") {
        var b = mk("div","cbanner");
        b.innerHTML = '<span>⚽</span><div><strong>লাইভ খেলাধুলা</strong>ক্রিকেট ও ফুটবল ম্যাচ চলাকালীন এই স্টেশনগুলোতে ধারাবিবরণী শুনুন</div>';
        grid.parentNode.insertBefore(b, grid);
    }

    var list;
    if (cat === "fav") {
        var fn = getFavs(); list = [];
        ["popular","islamic","music","news","english","kolkata"].forEach(function(k) {
            S[k].forEach(function(st) { if(fn.indexOf(st.n)!==-1) list.push(st); });
        });
        if (!list.length) { grid.innerHTML = '<div style="grid-column:1/-1;text-align:center;padding:50px 20px;color:rgba(245,243,240,.2)"><div style="font-size:40px;margin-bottom:12px">❤️</div><div style="font-size:15px">প্রিয় স্টেশন নেই</div><div style="font-size:13px;margin-top:6px;color:rgba(245,243,240,.15)">স্টেশনে লং-প্রেস করে যোগ করুন</div></div>'; return; }
    } else if (cat === "all") {
        var d=[], w=[], seen={};
        ["popular","islamic","music","news","english","kolkata"].forEach(function(k) {
            S[k].forEach(function(st) { if(!seen[st.n]){seen[st.n]=true; if(st.s) d.push(st); else w.push(st);} });
        });
        list = d.concat(w);
    } else { list = S[cat] || []; }

    list.forEach(function(st) {
        var isWeb = !st.s;
        var card = mk("div", "card" + (isWeb ? " web" : ""));
        card.style.background = "linear-gradient(145deg," + st.c[0] + "," + st.c[1] + ")";
        card.setAttribute("role","button");
        card.setAttribute("tabindex","0");
        card.setAttribute("aria-label", st.n + " " + st.f);

        var top = mk("div","card-top");
        var icon = mk("div","card-icon"); icon.textContent = st.i;
        var live = mk("div","card-live");
        top.appendChild(icon); top.appendChild(live);

        var info = mk("div","card-info");
        info.appendChild(mk("div","card-name",st.n));
        info.appendChild(mk("div","card-freq",st.f));

        if (isWeb) { var wt = mk("div","web-tag","WEB"); card.appendChild(wt); }
        if (isFav(st.n)) card.appendChild(mk("div","fav-dot"));
        card.appendChild(top); card.appendChild(info);

        if (curStation && curStation.n === st.n && curStation.s === st.s) { card.classList.add("playing"); curCard = card; }

        card.onclick = function() { if(isWeb) window.open(st.w,"_blank"); else playStation(st, card); };

        var pt;
        card.addEventListener("touchstart", function() { pt = setTimeout(function() {
            var added = toggleFav(st.n);
            var dot = card.querySelector(".fav-dot");
            if(added && !dot) card.appendChild(mk("div","fav-dot"));
            else if(!added && dot) dot.remove();
            card.style.transform = "scale(1.04)"; setTimeout(function(){card.style.transform="";},200);
        }, 600); }, {passive:true});
        card.addEventListener("touchend", function(){clearTimeout(pt);}, {passive:true});
        card.addEventListener("touchmove", function(){clearTimeout(pt);}, {passive:true});
        card.addEventListener("keydown", function(e){if(e.key==="Enter"||e.key===" "){e.preventDefault();card.onclick();}});

        grid.appendChild(card);
    });
}

function setPlayer(name, freq, on, buffering) {
    var p = document.getElementById("player");
    var pn = document.getElementById("pName");
    var pf = document.getElementById("pFreq");
    var pv = document.getElementById("volSlider");
    p.className = on ? "player on" : "player idle";
    pn.textContent = name; pn.className = "p-name" + (buffering ? " buffering" : "");
    pf.textContent = freq;
    if (pv) pv.style.display = on ? "block" : "none";
}

function updateMediaSession(st) {
    if (!("mediaSession" in navigator)) return;
    navigator.mediaSession.metadata = new MediaMetadata({
        title: st.n,
        artist: st.f,
        album: "Bangla Radio",
        artwork: [
            { src: "icon-192.png", sizes: "192x192", type: "image/png" },
            { src: "icon-512.png", sizes: "512x512", type: "image/png" }
        ]
    });
    navigator.mediaSession.setActionHandler("play", function(){if(audio)audio.play();});
    navigator.mediaSession.setActionHandler("pause", function(){stopAll();});
    navigator.mediaSession.setActionHandler("stop", function(){stopAll();});
    navigator.mediaSession.setActionHandler("previoustrack", function(){switchStation(-1);});
    navigator.mediaSession.setActionHandler("nexttrack", function(){switchStation(1);});
}

function clearConnectTimer() { if(connectTimer){clearInterval(connectTimer);connectTimer=null;} connectSec=0; }

function playStation(st, card) {
    if (curStation && curStation.n===st.n && curStation.s===st.s) { stopAll(); return; }
    stopAll();
    curStation = st; curCard = card;
    if(card) card.classList.add("loading");
    saveLastPlayed(st);
    setPlayer("সংযোগ হচ্ছে...", st.n, true, true);

    connectSec = 0;
    connectTimer = setInterval(function() {
        connectSec++;
        if (audio && curStation === st) {
            if (connectSec < 15) { setPlayer("সংযোগ হচ্ছে " + connectSec + "s", st.n, true, true); }
            else {
                clearConnectTimer();
                if(audio){audio.pause();audio.src="";audio=null;}
                if(card) card.classList.remove("loading","playing");
                setPlayer("ধীর নেটওয়ার্ক","",true,true);
                setTimeout(function(){if(!curStation)setPlayer("","",false,false);},3000);
                curStation=null;curCard=null;
            }
        } else clearConnectTimer();
    }, 1000);

    audio = new Audio();
    audio.preload = "none";
    audio.volume = document.getElementById("volSlider").value / 100;

    // Low-end device optimization: minimal memory, no unnecessary buffering
    if (audio.mozAutoplayPolicy !== undefined) audio.mozAutoplayPolicy = "allowed";

    var a = audio, retry = 0;

    function onReady() {
        if(audio!==a) return; clearConnectTimer();
        if(card){card.classList.remove("loading");card.classList.add("playing");}
        setPlayer(st.n, st.f, true, false);
        updateMediaSession(st);
    }

    a.addEventListener("canplay", function h() { a.removeEventListener("canplay",h); onReady(); });
    a.addEventListener("playing", function() { onReady(); });

    a.addEventListener("error", function() {
        if(audio!==a) return;
        if(retry<2){retry++;setPlayer("চেষ্টা "+retry+"/2",st.n,true,true);setTimeout(function(){if(audio!==a)return;a.load();a.play().catch(function(){});},2000);return;}
        clearConnectTimer();
        if(card)card.classList.remove("loading","playing");
        setPlayer("সংযোগ হয়নি","",true,true);
        setTimeout(function(){if(!curStation)setPlayer("","",false,false);},3000);
        curStation=null;curCard=null;audio=null;
    });

    a.addEventListener("waiting", function() {
        if(audio!==a)return; if(card)card.classList.add("loading");
        setPlayer("বাফারিং...", st.n, true, true);
    });

    // Auto-reconnect if stream drops (common on BD networks)
    a.addEventListener("stalled", function() {
        if(audio!==a) return;
        setPlayer("পুনঃসংযোগ...", st.n, true, true);
        setTimeout(function(){
            if(audio===a && a.paused) { a.load(); a.play().catch(function(){}); }
        }, 3000);
    });

    a.src = st.s;
    a.play().catch(function(){
        if(audio!==a)return;clearConnectTimer();
        if(card)card.classList.remove("loading","playing");
        setPlayer("প্লে হচ্ছে না","",true,true);
        setTimeout(function(){if(!curStation)setPlayer("","",false,false);},3000);
        curStation=null;curCard=null;audio=null;
    });
}

function stopAll() {
    clearConnectTimer();
    if(audio){
        audio.pause();
        audio.removeAttribute("src");
        audio.load(); // release network connection and memory
        audio=null;
    }
    if(curCard){curCard.classList.remove("playing","loading");curCard=null;}
    curStation=null; clearSleepTimer();
    setPlayer("","",false,false);
}

function clearSleepTimer(){if(sleepTimer){clearTimeout(sleepTimer);sleepTimer=null;}var b=document.getElementById("btnSleep");if(b)b.textContent="🌙";}
function cycleSleep(){
    var t=[0,15,30,60],l=["🌙","15m","30m","60m"],b=document.getElementById("btnSleep"),
        c=parseInt(b.dataset.m||"0"),nx=t[(t.indexOf(c)+1)%t.length];
    b.dataset.m=nx;clearSleepTimer();
    if(nx>0){b.textContent=l[t.indexOf(nx)];sleepTimer=setTimeout(function(){stopAll();b.textContent="🌙";b.dataset.m="0";},nx*60000);}
    else b.textContent="🌙";
}

function shareApp(){
    if(navigator.share) navigator.share({title:"বাংলা রেডিও",text:"বাংলাদেশের সব FM রেডিও — ফ্রি!",url:"https://shamim-ashraf.github.io/Bangla-Radio/"});
    else{var t=document.createElement("textarea");t.value="https://shamim-ashraf.github.io/Bangla-Radio/";document.body.appendChild(t);t.select();document.execCommand("copy");document.body.removeChild(t);
    var b=document.getElementById("btnShare");b.textContent="✓";setTimeout(function(){b.textContent="↗";},1500);}
}

function getAllPlayable() {
    var list = [], seen = {};
    ["popular","islamic","music","news","english","kolkata"].forEach(function(k) {
        S[k].forEach(function(st) { if(st.s && !seen[st.n]){seen[st.n]=true;list.push(st);} });
    });
    return list;
}

function switchStation(dir) {
    var list = getAllPlayable();
    if (!list.length) return;
    var idx = 0;
    if (curStation) {
        for (var i=0; i<list.length; i++) { if(list[i].n===curStation.n){idx=i;break;} }
        idx = (idx + dir + list.length) % list.length;
    }
    var st = list[idx];
    var cards = document.querySelectorAll(".card");
    var found = null;
    cards.forEach(function(c) { var nm=c.querySelector(".card-name"); if(nm&&nm.textContent===st.n) found=c; });
    playStation(st, found);
}

document.getElementById("btnPrev").onclick = function(){switchStation(-1);};
document.getElementById("btnNext").onclick = function(){switchStation(1);};
document.getElementById("btnStop").onclick = stopAll;
document.getElementById("btnSleep").onclick = cycleSleep;
document.getElementById("btnShare").onclick = shareApp;
document.getElementById("volSlider").addEventListener("input",function(){if(audio)audio.volume=this.value/100;});

document.getElementById("tabs").addEventListener("click",function(e){
    var tab=e.target.closest(".tab"); if(!tab)return;
    document.querySelectorAll(".tab").forEach(function(t){t.classList.remove("active");});
    tab.classList.add("active"); renderGrid(tab.dataset.cat);
});

var deferredPrompt;
window.addEventListener("beforeinstallprompt",function(e){e.preventDefault();deferredPrompt=e;var b=document.getElementById("btnInstall");if(b)b.style.display="flex";});
var ib=document.getElementById("btnInstall");
if(ib)ib.onclick=function(){if(deferredPrompt){deferredPrompt.prompt();deferredPrompt.userChoice.then(function(){deferredPrompt=null;document.getElementById("btnInstall").style.display="none";});}};

renderGrid("all");

// Auto-play on first interaction (browsers block auto-play without user gesture)
function autoPlayOnce() {
    document.removeEventListener("click", autoPlayOnce);
    document.removeEventListener("touchstart", autoPlayOnce);
    if (curStation) return; // already playing
    var last = getLastPlayed();
    var target = (last && last.s) ? last : S.popular[0];
    if (target && target.s) {
        var cards = document.querySelectorAll(".card");
        var found = null;
        cards.forEach(function(c) {
            var nm = c.querySelector(".card-name");
            if (nm && nm.textContent === target.n) found = c;
        });
        playStation(target, found);
    }
}

// Try silent auto-play first (works on desktop, some mobile browsers)
setTimeout(function(){
    var last = getLastPlayed();
    var target = (last && last.s) ? last : S.popular[0];
    if (target && target.s) {
        var test = new Audio();
        test.volume = 0;
        test.src = target.s;
        var p = test.play();
        if (p) p.then(function(){
            // Auto-play allowed! Play for real
            test.pause(); test.removeAttribute("src"); test.load();
            var cards = document.querySelectorAll(".card");
            var found = null;
            cards.forEach(function(c) {
                var nm = c.querySelector(".card-name");
                if (nm && nm.textContent === target.n) found = c;
            });
            playStation(target, found);
        }).catch(function(){
            // Auto-play blocked — wait for first tap anywhere on page
            test.removeAttribute("src"); test.load();
            setPlayer("▶ ট্যাপ করুন — " + target.n, target.f || "", true, false);
            document.addEventListener("click", autoPlayOnce, {once:true});
            document.addEventListener("touchstart", autoPlayOnce, {once:true,passive:true});
        });
    }
}, 300);

if("serviceWorker" in navigator) navigator.serviceWorker.register("sw.js").catch(function(){});
